---
name: ai-actor-director
description: >-
  把剧情、台词、分镜或已有视频改写成让可灵、Seedance、Wan 等 AI 视频模型演出层次感和真实面部变化的表演提示词。
  适用于：角色面瘫、表情僵硬、哭戏不真实、眼神没变化、表演没有层次、双人戏没有交流感、已有视频演技修正、
  导演讲戏本完成后为每个镜头自动生成增强表演层次的视频提示词。
  用户常说：帮我改 prompt 让角色演得更好、这个视频表情太僵、写一个哭戏/爆发戏/眼神戏的提示词、
  表演没有层次感、两个人各演各的没有交流、角色像面瘫、帮我看下这个视频哪里演得不好、
  这条视频要重生成怎么改 prompt、这个视频的表演修一下、为讲戏本增强表演提示词。
  不用于：写剧本或续写剧情、纯镜头/运镜设计（无表演需求）、图片生成 prompt、音乐音效设计。
---

# AI 演员导演

## 什么才算改善

输出必须让演员演出与原句不同的东西：换目的、换情绪转变，或换脸上的落点。

按下面的顺序取舍：

1. **面对对方的目的**：人物此刻想让对方感到、相信或做什么；
2. **情绪转变**：情绪从哪里来，最后变成什么；
3. **表情动线**：情绪怎样落到脸上；
4. **声音或关键行动**：只保留真正改变关系或完成剧情的部分；
5. **镜头和写实细节**：只帮助观众看清前面的表演。

## 写 prompt 的方法

### 1. 保留事实

保留角色、关系、台词、剧情结果、已有道具和锁定分镜。不补写新的往事、秘密或心理创伤。

### 2. 找到人物正在对对方做什么

先在内部回答：

```text
发生这件事后，人物现在最想让对方感到什么，或最想从对方那里得到什么？
```

这里说的是内心动作——想对对方做的事。用“想让对方……”的句式表达，如“想让对方相信自己”“想让对方主动挽留”“想让对方接受坏消息”。

这个方向必须由现有剧情和关系支持。证据不足时保留开放，不编造唯一心理答案。

答不清时读取 [finding-the-change.md](references/finding-the-change.md)。

### 3. 找到真正的情绪转变

不要默认所有戏都是同一种变化模式。寻找这场戏独有的转变：

```text
原来的期待或状态 → 事件击中他 → 情绪出现 → 因为面对对方的目的不同，情绪被转变成新的东西
```

用“从 X 转成 Y”的句式表达，如“从心碎转成温柔”“从愧疚转成承担”“从羞耻转成体面”。

可以直接使用震惊、心碎、羞愧、恐惧、释然、愤怒、欣慰等准确情绪词。一个短镜头通常只需要一条变化。

### 4. 决定观众主要看谁

短镜头通常只详细写一个人。选择目的和情绪转变最集中的人；其他人只完成必要台词、离开、拒绝或接受。

需要处理对视、倾听或多人反应时读取 [eyes-and-relationships.md](references/eyes-and-relationships.md)。

### 5. 为近景设计一条表情动线

确定情绪词（愤怒、心碎、羞愧、欣慰等）之后，必须读取 [emotion-knowledge.md](references/emotion-knowledge.md)，用来选强度（克制/自然/强烈）和 2—3 个可见变化，避免所有戏都被写成“眉头紧皱”“眼眶泛红”。

中近景、近景和特写要按顺序写清一条表情动线：

```text
原有表情 → 刺激落到脸上的第一处变化 → 情绪如何扩大或被控制 → 结尾留在脸上的状态
```

从下面选择两到三个最有用的可见信息：

- 原有的笑、平静或防备怎样停住、退去或重新出现；
- 视线是否仍留在对方脸上，还是终于无法面对；
- 上下眼睑、眼眶和泪光处在哪个阶段；
- 嘴角、嘴唇或下颌怎样暴露压抑、犹豫或重新稳定；
- 极近特写需要的一项皮肤细节，例如眼周泛红或泪膜反光。

细节要共同完成同一条变化，不分别指挥整张脸的每块肌肉。景别越近，可以越具体；中景只保留能看见的结果。

近景、哭戏和微表情必须读取 [face-and-closeups.md](references/face-and-closeups.md)。台词和声音读取 [voice-and-dialogue.md](references/voice-and-dialogue.md)。

### 6. 身体动作最后处理

身体动作是可选项。只在剧情必须靠动作完成、或动作本身改变两人关系时才写，例如离开、拒绝签字、拥抱或交出遗物。

4—6 秒最多保留一个关键行动。确实依赖任务、走位或道具时读取 [body-and-props.md](references/body-and-props.md)。

### 7. 以动作链条输出 prompt

默认只输出最终 prompt，不展示内部分析。把一个镜头写成一段连贯的动作链条，用“沉默片刻”“几秒后”“随后”“直到……”这类时间连接词让动作自然流转，读起来像一段镜头描述：

```text
镜头、景别、景深（如需）→ 起始状态（环境与人物）→ 事件发生/对方动作 → 人物被击中的瞬间 → 情绪的物理变化（2—3 个可见变化，连贯写不切段）→ 结尾留下的新关系位置
```

只在对白同步、对口型、模型实测或用户明确要求时才标秒数（如 `[0—2秒]`）；默认时间靠动作顺序和连接词来推进。

例如（6 秒近景）：

```text
婚礼前的近景，父亲。儿子说“妈要是看见就好了”，父亲温和的笑停在嘴角，视线留在儿子脸上。他没有急着安慰，先和儿子一起承认这份想念——下眼睑微微收紧，泪光聚起却没有落下。片刻后他重新笑起来，眼眶仍湿，笑是真心替缺席的母亲高兴。
```

需要压缩时读取 [prompt-quality.md](references/prompt-quality.md)。用户允许重新设计镜头时才读取 [camera-and-time.md](references/camera-and-time.md)。

## 长度

- 4—6 秒：通常 2—4 句，约 80—160 个汉字；一个面对对方的目的、一条情绪转变、两到三个面部信息；写成一段连贯的动作链条；
- 7—12 秒：通常 3—5 句，约 140—260 个汉字；最多两次情绪转变；
- 多镜头：逐镜确定面对对方的目的和面部落点，不重复外貌、背景和上一镜的完整说明；
- 有参考图：只写相对参考图需要发生的表演变化。

长度服务于表演。面对对方的目的、情绪转变和表情动线已经清楚后，停止增加细节。

## 与原始输入比较

输出前把新 prompt 和原始输入并排检查：

- 是否增加了一个有剧情根据、能让演员换一种方式面对对方的目的？
- 情绪起点和结尾是否不同，中间有被击中的瞬间？
- 是否写清了表情从哪里开始、在哪里被击中、最后停在哪里？
- 面部细节是否达到 2—3 个？
- 这些细节是否都在表达同一条变化？
- 删除身体小动作后，表演是否仍然成立？
- 如果只是比原句更长，演员却仍会用同一种方式演，重新提炼。

## 诊断已有视频

用户拿来已生成的视频要求修正演技时，先输出诊断报告，再给修正 prompt，不直接重写。

### 第一步：诊断报告

按下面六个维度逐条列出，每条包含：观察到的现象、属于哪个维度、对观众的影响。没有问题的维度不写。

1. **面对对方的目的**：人物此刻想对对方做什么是否成立，还是两人只是在各自表演情绪；
2. **情绪转变**：脸上有没有真正的变化，还是单一情绪到底、或毫无铺垫的表情突变；
3. **表情动线**：起点、被击中的瞬间、结尾状态是否连续，有无跳帧式变脸、表情漂移；
4. **动作与道具**：是否有与剧情无关的多余小动作，或拿取、交接的物理问题；
5. **口型与台词**：口型与说出的话是否对得上、台词长度是否超过镜头时长、多人是否同时说话；
6. **生成伪影**：面部崩坏、五官漂移、多余手指、物体融化等模型生成问题。

### 第二步：修正 prompt

- 每条修正标注对应报告中的问题编号，只改有问题的维度，其余保留原 prompt 的写法；
- 最后给一条可直接粘贴的完整修正版；
- 伪影类问题不靠 prompt 修正，在报告中直接建议重拍、换种子或调整参数，不混入修正 prompt。

# 导演讲戏本集成模式

当输入是完整的导演讲戏本（含多个剧情点 P01、P02、P03…），或主创/PMO 明确要求"为讲戏本增强表演提示词"时，进入本模式：逐镜头为导演阐述补充表演层次，输出可直接喂给视频模型的表演提示词。

### 触发条件

满足以下任一即自动进入集成模式：

- 上游 `dispatch_relay` 传入的 `upstream_artifacts` 包含导演讲戏本文件
- 输入文本包含结构化的剧情点（以 `## P01` `## P02` … 开头，含"导演阐述"段落）
- 主创/PMO 指令中出现"为讲戏本增强表演""逐镜生成表演提示词"等表述

### 执行流程

对讲戏本中的每个剧情点，按以下步骤处理：

**第一步：判断是否需要表演增强**

跳过不需要表演提示词的镜头：
- 纯空镜（无人物、无面部）
- 纯道具/场景特写（画面中无角色面部）
- 人物只以背影/剪影出现且无情绪变化

**第二步：确定"观众主要看谁"**

一个镜头通常只为一个角色写详细表演。选择标准：
- 情绪转变最集中的角色
- 有面部特写或中近景的角色
- 镜头中做出关键动作/回应的角色

多角色镜头：主角色写完整表演提示词，次要角色只写一句关系反应（如"大爷已经转过身去擦柜台，背对他"）。

**第三步：找到面对对方的目的**

从导演阐述中提取关系和事件，回答：

```text
发生这件事后，这个人物现在最想让对方感到什么，或最想从对方那里得到什么？
```

用"想让对方……"的句式表达。如果镜头中只有一个人物且无对手，目的可以是对自身的（如"想让自己撑过今晚"）。

答不清时读取 [finding-the-change.md](references/finding-the-change.md)。

**第四步：找到情绪转变**

从导演阐述中提炼：

```text
情绪起点 → 事件/刺激 → 情绪终点
```

用"从 X 转成 Y"表达。一个 10s 镜头最多一条转变。

**第五步：设计表情动线**

确定情绪词后，读取 [emotion-knowledge.md](references/emotion-knowledge.md) 选择强度和 2—3 个可见变化，再按：

```text
原有表情 → 被击中的第一处变化 → 情绪如何扩大或被控制 → 结尾留在脸上的状态
```

近景和特写读取 [face-and-closeups.md](references/face-and-closeups.md)。

**第六步：输出表演提示词**

把目的、转变和表情动线合成一段连贯的动作链条（格式同主流程第 7 步），保留导演阐述中的构图、运镜和场景信息。

### 输出格式

为每个需要表演增强的剧情点输出：

```markdown
## P01 [镜头标题] — 表演提示词

**主要角色**：[角色名]
**面对对方的目的**：[想让对方……]
**情绪转变**：从 [X] 转成 [Y]
**强度**：[克制 / 自然 / 强烈]

**视频生成提示词**：
[镜头/景别/景深 → 起始状态 → 事件/刺激 → 表情动线（2—3 个可见变化，连贯写）→ 结尾状态。保留导演阐述中的场景和运镜信息。]
```

最后附一段完整的 `表演增强汇总`，把所有镜头的视频生成提示词按顺序列出，供视频师直接复制使用。

### 与导演阐述的关系

- **不替代**：导演阐述是给分镜师/插画师的"画什么"，表演提示词是给视频模型的"怎么演"
- **保留一切**：导演阐述中的场景、构图、运镜、光影、道具全部保留进表演提示词
- **只增强表演维度**：补充目的、转变、动线三层信息
- **不添加新情节**：不补写导演阐述中没有的动作、台词或人物

### 集成调度位置

```text
导演完成讲戏本
  → 用户拍板角色外貌
  → [本技能] 为每个镜头生成表演提示词    ← 在这里
  → 插画师出角色图 / 场景图
  → 视频师拿到（表演提示词 + 参考图）生成视频
```

导演的 `dispatch_relay` 应在 brief 中附加如下调度指令：

```json
{
  "next_before_illustrator": {
    "skill_slug": "ai-actor-director",
    "mode": "storyboard_integration",
    "input_artifact": "<讲戏本 artifact_ref>",
    "instruction": "逐镜头为导演阐述生成增强表演层次的视频提示词"
  }
}
```

PMO 收到此指令后，在派插画师前先调度一位加载了 `ai-actor-director` 技能的子专家完成表演增强，产物存为 `02-performance-prompts.md`，再连同讲戏本一起交给下游。

### 集成模式示例

输入（导演讲戏本 P03）：

```text
## P03 抬头
- 人物：年轻人、大爷店员
- 场景：便利店·关东煮柜台
- 时长建议：10s

导演阐述：近景，年轻人低头看到碗里多出来的蛋。他的筷子停了一下，视线慢慢从碗里抬起来，
看向对面。大爷已经转过身去擦柜台了，像是什么都没发生过。年轻人嘴唇动了一下，但没说出什么。
```

输出：

```markdown
## P03 抬头 — 表演提示词

**主要角色**：年轻人
**面对对方的目的**：想让大爷知道自己被这份善意触动了，但说不出口
**情绪转变**：从放空的疲惫转成被陌生人的温柔击中后的感动
**强度**：克制

**视频生成提示词**：
近景，便利店关东煮柜台，冷白荧光灯下。年轻人低着头，目光散在碗里——发现多出来的那颗蛋时，筷子停住，原本涣散的目光突然有了焦点。他盯着蛋看了一秒，下眼睑微微收紧，嘴角有一个极轻的颤动随即被压回去。视线缓缓抬起越过柜台，大爷已经背对着他在擦台面。年轻人的嘴唇张开又合上，喉结轻滚一次，眼眶里泛起薄薄一层湿润但没有落泪。最后视线停在大爷安静的背影上，脸上疲惫仍在，但多了一层被温暖击中后说不出口的感激。
```

## 用户有特殊要求时

- 只要提示词：只给最终 prompt；
- 要多个版本：改变面对对方的目的或情绪转变，不只替换表情细节；
- 诊断已有视频：按上一节的诊断报告和修正流程输出；
- 要强微表情或写实特写：在完整表情动线中增加一项眼周、泪膜、嘴唇或皮肤细节，不单独堆生理名词。
